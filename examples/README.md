# Examples

No runnable example exists yet. `examples/basic_usage.py`-style direct SDK
usage lands with OBS-13 (SDK instrumentation), once `sdk.FactLamaClient`
exists; a query-API example lands with OBS-07. This directory is
established now (OBS-01) so those additions have a place to go.
